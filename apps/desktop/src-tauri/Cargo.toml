[package]
name = "ai_hub_desktop"
version = "0.1.0"
description = "AI Hub desktop shell"
authors = ["AI Hub Contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }

[features]
default = []

